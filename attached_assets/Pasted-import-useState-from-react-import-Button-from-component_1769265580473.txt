import { useState } from 'react';
import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import { Card } from "@/components/ui/card";

export const EVChecklist = ({ loadId, loadData }) => {
  const [steps, setSteps] = useState({
    initialSms: false,     // Step 1
    tripMessage: false,    // Step 2
    puArrived: false,      // Step 3
    annexNotified: false,  // Step 4
    puDocsRequested: false, // Step 5
    brokerConfirmed: false, // Step 6
    transitMonitored: false, // Step 7
    delDocsRequested: false, // Step 8
    driverReleased: false,  // Step 9
    docsToEinstein: false,  // Step 10
    factoringSent: false,   // Step 11
    fuelLogged: false,      // Step 12
    brokerThankYou: false   // Step 13
  });

  return (
    <Card className="p-4 w-80 space-y-4">
      <h3 className="font-bold text-lg">Load Tracking SOP</h3>
      <div className="space-y-2">
        {/* Step 1: Automated SMS Trigger */}
        <div className="flex items-center space-x-2">
          <Checkbox checked={steps.initialSms} onCheckedChange={() => setSteps({...steps, initialSms: true})} />
          <label className="text-sm">1. Send Load Details (SMS)</label>
          <Button size="xs" variant="outline" onClick={() => triggerSms(loadId, 'INITIAL')}>Send</Button>
        </div>

        {/* Step 5: Document Verification */}
        <div className="border-t pt-2">
          <p className="text-xs font-semibold text-slate-500">Step 5: Pickup Documentation</p>
          <div className="flex gap-2 mt-1">
             <div className="h-10 w-10 bg-slate-100 rounded border flex items-center justify-center text-[10px]">BOL</div>
             <div className="h-10 w-10 bg-slate-100 rounded border flex items-center justify-center text-[10px]">PHOTO</div>
          </div>
        </div>

        {/* Step 11: Factoring Deadline Indicator */}
        <div className="bg-amber-50 p-2 rounded text-xs">
          <p className="font-bold text-amber-700">Factoring Deadline: 12:00 PM</p>
          <p>Current Time: {new Date().toLocaleTimeString()}</p>
        </div>
      </div>
    </Card>
  );
};